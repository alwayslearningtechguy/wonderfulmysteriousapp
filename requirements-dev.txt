# requirements-dev.txt — Development and test dependencies
# Updated to include specialized testing tools

# ── Existing test dependencies ──────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.25.3
httpx==0.28.1
anyio==4.13.0

# ── Specialized testing — Security ─────────────────────────────────────────
pip-audit==2.7.3          # Dependency vulnerability scanning
bandit==1.7.10            # Python static security analysis

# ── Specialized testing — Performance ──────────────────────────────────────
locust==2.32.2            # Load and performance testing

# ── Specialized testing — Cross-browser + Accessibility ────────────────────
pytest-playwright==0.5.2  # Playwright integration for pytest
axe-playwright-python==0.1.3  # axe-core accessibility scanning via Playwright
# After installing, run: playwright install chromium firefox
